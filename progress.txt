# Ralph Progress Log
Started: Mon Apr 13 10:35:34 EDT 2026

## Codebase Patterns
- Keep the active roadmap contract in a dedicated doc and link it from `README.md` and `docs/getting-started.md` so implementation work and user-facing navigation stay aligned.
- When parity decisions depend on Java and Python comparisons, record intentional .NET-native differences explicitly so later feature work targets capabilities rather than cross-language API cloning.
- For index metadata extensions, preserve `IndexDefinition.Prefix` as the primary write-path namespace and add new FT.CREATE metadata via separate strongly typed properties so document loading APIs stay backward compatible.
- Model optional FT.CREATE metadata as nullable index properties where `null` means Redis defaults and an empty collection means an explicit `0`-count clause, so default and disabled behaviors remain distinguishable in both code-first and YAML schema paths.
- Emit RediSearch field modifiers in parser-safe order: `INDEXEMPTY`/`INDEXMISSING` before `SORTABLE`, `UNF` immediately after `SORTABLE`, and `NOINDEX` after sortable modifiers; vector `INDEXMISSING` belongs inside the vector attribute payload rather than after the field definition.
- Keep YAML schema loading strict: accept either `index.prefix` or `index.prefixes` for namespace configuration, map upstream-style underscored option names through DTOs, and fail fast on unsupported keys instead of silently ignoring them.
- Index maintenance helpers should operate on `Schema.Index.Prefixes` with batched Redis key scans and deletes so JSON and HASH storage share one implementation while the RediSearch index definition remains intact.
- Database-wide RediSearch discovery helpers should stay static on `SearchIndex` and map Redis array replies into small named DTOs, which keeps environment inspection APIs available without requiring a schema-bound instance.
- When rebuilding `SearchSchema` from `FT.INFO`, collapse JSON fields back to alias-less definitions only when the Redis `attribute` matches the default alias for the JSON path; otherwise preserve the raw identifier path plus explicit alias so recreated schemas round-trip cleanly.
- JSON partial update helpers should accept only absolute JSONPath expressions like `$.field` or `$[0]`, trim and deduplicate paths before issuing Redis commands, and treat missing root documents as a `false` return instead of implicitly creating new JSON keys.
- HASH partial update helpers should reuse a hash-exists probe plus a single `HSET` batch, accept only trimmed top-level field names, and reject `null` values because HASH writes preserve omitted fields rather than expressing field deletion.
- New FT.SEARCH query types should reuse the shared return-field normalization and the standard `RETURN`/`LIMIT`/`DIALECT 2` argument layout, which keeps query-specific additions isolated to the search string or extra clauses.
- FT.AGGREGATE query builders should normalize schema-backed property references to the runtime query field name while still allowing APPLY and REDUCE aliases to flow through as plain `@alias` references in later pipeline stages.
- Redis-backed query integration tests should seed tiny HASH datasets with intentionally different match strength, wait on `num_docs` before asserting, and verify projected-field omission explicitly when a query narrows `RETURN` fields.

---

## 2026-04-13 10:36:39 EDT - US-001
- Implemented a new `docs/parity-roadmap.md` contract that compares current .NET, Java, and Python capabilities, labels gaps as required, deferred, or out of scope, and documents intentional .NET-native differences.
- Files changed: `docs/parity-roadmap.md`, `README.md`, `docs/getting-started.md`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet build redis-vl-dotnet.sln --no-restore` passed.
- Learnings for future iterations:
  - Patterns discovered: parity contract changes need the repo navigation docs updated in the same iteration or contributors will keep following stale roadmap references.
  - Gotchas encountered: the local repository did not have a `main` ref, so the PRD branch had to be created from the current HEAD instead.
  - Useful context: this repo already contains `docs/feature-analysis.md`, which is a good evidence source for future parity stories but should not replace the shorter roadmap contract doc.
---
## 2026-04-13 10:40:16 EDT - US-002
- Implemented multi-prefix index metadata support by adding an `IndexDefinition` overload that accepts multiple prefixes, preserving `Prefix` as the primary default key prefix, and emitting all configured prefixes in `FT.CREATE`.
- Files changed: `src/RedisVlDotNet/Schema/IndexDefinition.cs`, `src/RedisVlDotNet/Indexes/SearchIndexCommandBuilder.cs`, `tests/RedisVlDotNet.Tests/Schema/SearchSchemaTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexCommandBuilderTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `examples/JsonStorageExample/Program.cs`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchSchemaTests|FullyQualifiedName~SearchIndexCommandBuilderTests|FullyQualifiedName~SearchIndexIntegrationTests"` passed with Redis-backed integration tests skipped because the Redis search test environment was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: new schema metadata should be modeled in a way that leaves document load/fetch helpers compatible with existing prefix-based key generation unless the story explicitly changes write semantics.
  - Gotchas encountered: `SearchIndexInfo` currently exposes FT.INFO as a generic attribute bag, so integration assertions should prefer existing search/count helpers unless a story specifically adds typed info parsing.
  - Useful context: the JSON example is the current runnable example surface for schema metadata, so parity-facing index option examples can usually be extended there without creating a new sample app.
---
## 2026-04-13 10:43:26 EDT - US-003
- Implemented index-level `KeySeparator` and `Stopwords` metadata on `IndexDefinition`, added YAML parsing for `key_separator` and `stopwords`, and emitted the corresponding `SEPARATOR` and `STOPWORDS` clauses from `FT.CREATE`.
- Files changed: `src/RedisVlDotNet/Schema/IndexDefinition.cs`, `src/RedisVlDotNet/Schema/SearchSchema.cs`, `src/RedisVlDotNet/Indexes/SearchIndexCommandBuilder.cs`, `tests/RedisVlDotNet.Tests/Schema/SearchSchemaTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexCommandBuilderTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `examples/JsonStorageExample/Program.cs`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --no-build --filter "FullyQualifiedName~SearchSchemaTests|FullyQualifiedName~SearchIndexCommandBuilderTests|FullyQualifiedName~SearchIndexIntegrationTests"` passed with Redis-backed integration tests skipped because the Redis search test environment was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: index-level schema options fit best on `IndexDefinition`, while YAML keeps upstream-compatible names like `key_separator` and `stopwords`.
  - Gotchas encountered: Redis default stopwords are represented by omitting the `STOPWORDS` clause entirely, so the model needs `null` for default behavior and an empty collection for `STOPWORDS 0`.
  - Useful context: `SearchIndexInfo` can still validate FT.INFO metadata for integration tests by inspecting the raw `index_definition` and `stopwords_list` bags even before typed parsing exists.
---
## 2026-04-13 10:48:25 EDT - US-004
- Implemented the remaining typed schema parity options for FT.CREATE by adding index-level flags (`MAXTEXTFIELDS`, `TEMPORARY`, `NOOFFSETS`, `NOHL`, `NOFIELDS`, `NOFREQS`, `SKIPINITIALSCAN`) plus field-level modifiers for text, tag, numeric, geo, and vector definitions (`WEIGHT`, `WITHSUFFIXTRIE`, `INDEXEMPTY`, `INDEXMISSING`, `NOINDEX`, `UNF`).
- Files changed: `src/RedisVlDotNet/Schema/IndexDefinition.cs`, `src/RedisVlDotNet/Schema/TextFieldDefinition.cs`, `src/RedisVlDotNet/Schema/TagFieldDefinition.cs`, `src/RedisVlDotNet/Schema/NumericFieldDefinition.cs`, `src/RedisVlDotNet/Schema/GeoFieldDefinition.cs`, `src/RedisVlDotNet/Schema/VectorFieldDefinition.cs`, `src/RedisVlDotNet/Indexes/SearchIndexCommandBuilder.cs`, `tests/RedisVlDotNet.Tests/Schema/SearchSchemaTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexCommandBuilderTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `examples/JsonStorageExample/Program.cs`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchSchemaTests|FullyQualifiedName~SearchIndexCommandBuilderTests|FullyQualifiedName~SearchIndexIntegrationTests"` passed with Redis-backed integration tests skipped because the Redis search test environment was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: RediSearch field modifier ordering is parser-sensitive, so `SearchIndexCommandBuilder` should keep advanced field options emitted in a canonical order instead of appending flags ad hoc.
  - Gotchas encountered: `dotnet test --no-build` can reuse a stale test assembly after assertion-only edits, so rerun without `--no-build` if failure output disagrees with the current source.
  - Useful context: `FT.INFO` exposes advanced index flags in `index_options` and field modifiers in the nested `attributes` array, which is enough for broad integration assertions before adding typed info models.
---
## 2026-04-13 10:52:27 EDT - US-005
- Implemented advanced YAML schema loading for multi-prefix index metadata, advanced index flags, and advanced text/tag/numeric/geo/vector field options, while tightening the loader to reject unsupported keys and ambiguous prefix declarations.
- Files changed: `src/RedisVlDotNet/Schema/SearchSchema.cs`, `tests/RedisVlDotNet.Tests/Schema/SearchSchemaTests.cs`, `tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj`, `tests/RedisVlDotNet.Tests/Schema/Fixtures/basic-schema.yaml`, `tests/RedisVlDotNet.Tests/Schema/Fixtures/advanced-schema.yaml`, `tests/RedisVlDotNet.Tests/Schema/Fixtures/unsupported-schema.yaml`, `examples/JsonStorageExample/Program.cs`, `examples/JsonStorageExample/JsonStorageExample.csproj`, `examples/JsonStorageExample/schema.yaml`, `examples/JsonStorageExample/README.md`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter FullyQualifiedName~SearchSchemaTests` passed. `dotnet test redis-vl-dotnet.sln` passed with 17 Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally. `dotnet build redis-vl-dotnet.sln --no-restore` passed.
- Learnings for future iterations:
  - Patterns discovered: checked-in YAML fixtures plus `CopyToOutputDirectory` are the cleanest way to validate `FromYamlFile(...)` behavior without temp-file setup in tests.
  - Gotchas encountered: changing shared parsing helpers like `ParseOptionalInt(...)` affects vector attribute parsing too, so schema test runs should happen before broader solution checks.
  - Useful context: the JSON storage sample can exercise YAML parity stories by loading `schema.yaml` from `AppContext.BaseDirectory`, which keeps the example runnable after `dotnet build` and `dotnet run`.
---
## 2026-04-13 10:56:00 EDT - US-006
- Implemented `SearchIndex.Clear/ClearAsync` to scan each configured schema prefix, delete matching Redis keys in batches, and preserve the underlying RediSearch index definition for both JSON and HASH storage.
- Files changed: `src/RedisVlDotNet/Indexes/SearchIndex.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexAsyncTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `docs/getting-started.md`, `examples/JsonStorageExample/Program.cs`, `examples/JsonStorageExample/README.md`, `examples/README.md`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchIndexAsyncTests|FullyQualifiedName~SearchIndexIntegrationTests"` passed with Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally. `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test redis-vl-dotnet.sln --no-restore` passed with 19 Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: index maintenance APIs can stay storage-agnostic when they operate on schema prefixes and Redis keys instead of branching on JSON versus HASH payload format.
  - Gotchas encountered: broad workspace status may include unrelated untracked Ralph artifacts and user edits, so commit only the story-specific files instead of sweeping the full worktree.
  - Useful context: `CountQuery` with no filter already maps to `*`, which is the simplest way to poll for an empty index after maintenance operations.
---
## 2026-04-13 11:53:29 EDT - US-007
- Implemented database-level `SearchIndex.List/ListAsync` helpers backed by `FT._LIST`, plus a `SearchIndexListItem` parser that exposes each discovered index name.
- Files changed: `src/RedisVlDotNet/Indexes/SearchIndex.cs`, `src/RedisVlDotNet/Indexes/SearchIndexListItem.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexAsyncTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexCommandBuilderTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `examples/JsonStorageExample/Program.cs`, `examples/JsonStorageExample/README.md`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchIndexAsyncTests|FullyQualifiedName~SearchIndexCommandBuilderTests|FullyQualifiedName~SearchIndexIntegrationTests"` passed with Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally. `dotnet build redis-vl-dotnet.sln --no-restore` passed.
- Learnings for future iterations:
  - Patterns discovered: database-wide index discovery belongs on static `SearchIndex` helpers because it does not depend on a schema instance.
  - Gotchas encountered: `FT._LIST` returns a flat Redis array of names, so parsing should fail fast on blank entries instead of silently producing unusable results.
  - Useful context: the existing JSON storage example is still the easiest place to demonstrate operational helpers like index listing without adding a new sample app.
---
## 2026-04-13 11:59:47 EDT - US-008
- Implemented `SearchIndex.FromExisting/FromExistingAsync`, added FT.INFO-to-schema reconstruction for supported index metadata and field definitions, and reused the rediscovered index in the JSON sample.
- Files changed: `src/RedisVlDotNet/Indexes/SearchIndex.cs`, `src/RedisVlDotNet/Indexes/SearchIndexSchemaBuilder.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexAsyncTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `examples/JsonStorageExample/Program.cs`, `examples/JsonStorageExample/README.md`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchIndexAsyncTests|FullyQualifiedName~SearchIndexIntegrationTests"` passed with Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally. `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test redis-vl-dotnet.sln --no-restore` passed with 21 Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: FT.INFO schema reconstruction fits best as an internal mapper that normalizes Redis metadata back into the existing strongly typed schema records, keeping the public API surface small.
  - Gotchas encountered: JSON FT.INFO rows expose both `identifier` and `attribute`, and only the default alias case should collapse back to a short field name; custom JSON aliases need the raw path preserved.
  - Useful context: no `AGENTS.md` files were present under the edited paths, so there was no area-local agent guidance to update for this iteration.
---
## 2026-04-13 12:04:01 EDT - US-009
- Implemented JSON partial update helpers on `SearchIndex` for updates by key and by id, backed by validated absolute JSONPath expressions and missing-document detection that returns `false` instead of creating new JSON keys.
- Files changed: `src/RedisVlDotNet/Indexes/SearchIndex.cs`, `src/RedisVlDotNet/Indexes/JsonPartialUpdate.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexAsyncTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `examples/JsonStorageExample/Program.cs`, `examples/JsonStorageExample/README.md`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchIndexAsyncTests|FullyQualifiedName~SearchIndexIntegrationTests"` passed with Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally. `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test redis-vl-dotnet.sln --no-restore` passed with 22 Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: JSON partial update APIs fit the existing `SearchIndex` lifecycle best when they reuse key/id resolution and accept a small path/value descriptor rather than a separate command-builder abstraction.
  - Gotchas encountered: passing an array directly to `LoadJsonAsync(...)` binds to the single-document overload, so example and test code should call the batch overload explicitly with `IEnumerable<T>`.
  - Useful context: no `AGENTS.md` files were present under the edited paths, so there was no area-local agent guidance to update for this iteration.
---
## 2026-04-13 12:08:09 EDT - US-010
- Implemented HASH partial update helpers on `SearchIndex` for updates by key and by id, backed by normalized top-level field names, a missing-document check that returns `false`, and a single batched `HSET` write that preserves untouched HASH fields.
- Files changed: `src/RedisVlDotNet/Indexes/SearchIndex.cs`, `src/RedisVlDotNet/Indexes/HashPartialUpdate.cs`, `src/RedisVlDotNet/Indexes/HashDocumentMapper.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexAsyncTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `examples/VectorSearchExample/Program.cs`, `examples/VectorSearchExample/README.md`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchIndexAsyncTests|FullyQualifiedName~SearchIndexIntegrationTests"` passed with Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally. `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test redis-vl-dotnet.sln --no-restore` passed with 23 Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: HASH partial update APIs fit the existing `SearchIndex` lifecycle when they mirror the JSON by-key and by-id entry points but translate updates into one `HSET` batch after validating trimmed, unique field names.
  - Gotchas encountered: HASH updates cannot express deletion with the current mapper, so `null` needs to fail fast instead of silently dropping fields from the update payload.
  - Useful context: there are still no `AGENTS.md` files under the edited paths, so reusable area guidance belongs in the progress log until local agent docs are introduced.
---
## 2026-04-13 12:11:56 EDT - US-011
- Implemented a first-class `TextQuery` model with explicit text input, normalized return fields, and pagination; added `FT.SEARCH` argument generation plus raw and typed `SearchIndex` execution overloads.
- Files changed: `src/RedisVlDotNet/Queries/TextQuery.cs`, `src/RedisVlDotNet/Indexes/SearchQueryCommandBuilder.cs`, `src/RedisVlDotNet/Indexes/SearchIndex.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchQueryCommandBuilderTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexAsyncTests.cs`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchQueryCommandBuilderTests|FullyQualifiedName~SearchIndexAsyncTests"` passed. `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test redis-vl-dotnet.sln --no-restore` passed with 23 Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: new FT.SEARCH query models can stay small when they reuse the common return-field and paging conventions already established by `FilterQuery` and related builders.
  - Gotchas encountered: the async `SearchIndex` recorder tests surface nullable assertion warnings if command arguments are rendered with raw `ToString()` calls, so test assertions should normalize possible nulls explicitly.
  - Useful context: there are still no `AGENTS.md` files under the edited paths, so there was no area-local agent guidance to update for this iteration.
---
## 2026-04-13 12:15:32 EDT - US-012
- Implemented Redis-backed `TextQuery` integration coverage for deterministic ranking, projected raw fields, and typed result mapping; updated the JSON storage example plus getting-started/example docs to demonstrate `TextQuery`.
- Files changed: `tests/RedisVlDotNet.Tests/Indexes/SearchIndexSeedData.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexIntegrationTests.cs`, `examples/JsonStorageExample/Program.cs`, `examples/JsonStorageExample/README.md`, `examples/README.md`, `docs/getting-started.md`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchIndexIntegrationTests|FullyQualifiedName~SearchQueryCommandBuilderTests|FullyQualifiedName~SearchIndexAsyncTests|FullyQualifiedName~SearchResultMappingTests"` passed with Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally. `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test redis-vl-dotnet.sln --no-restore` passed with 25 Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: deterministic `TextQuery` ranking tests are easiest to keep stable when seed titles use clearly different term frequency and the query projects only the fields each assertion needs.
  - Gotchas encountered: this environment still does not expose `REDIS_VL_REDIS_URL`, so new Redis-backed tests are discovered and compiled but cannot execute end-to-end locally until that variable points at a RediSearch-capable instance.
  - Useful context: there are still no `AGENTS.md` files under the edited paths, so reusable area guidance remains centralized in `progress.txt`.
---
## 2026-04-13 12:21:39 EDT - US-013
- Implemented a first-class `AggregationQuery` model with typed `APPLY`, `GROUPBY`, `REDUCE`, `SORTBY`, and `LOAD` pipeline components, added `FT.AGGREGATE` command generation, and exposed raw aggregation execution methods on `SearchIndex`.
- Files changed: `src/RedisVlDotNet/Queries/AggregationApply.cs`, `src/RedisVlDotNet/Queries/AggregationGroupBy.cs`, `src/RedisVlDotNet/Queries/AggregationQuery.cs`, `src/RedisVlDotNet/Queries/AggregationReducer.cs`, `src/RedisVlDotNet/Queries/AggregationReducerArgument.cs`, `src/RedisVlDotNet/Queries/AggregationSortBy.cs`, `src/RedisVlDotNet/Queries/AggregationSortField.cs`, `src/RedisVlDotNet/Indexes/SearchQueryCommandBuilder.cs`, `src/RedisVlDotNet/Indexes/SearchIndex.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchQueryCommandBuilderTests.cs`, `tests/RedisVlDotNet.Tests/Indexes/SearchIndexAsyncTests.cs`, `prd.json`, `progress.txt`
- Quality checks run and result: `dotnet test tests/RedisVlDotNet.Tests/RedisVlDotNet.Tests.csproj --filter "FullyQualifiedName~SearchQueryCommandBuilderTests|FullyQualifiedName~SearchIndexAsyncTests"` passed. `dotnet build redis-vl-dotnet.sln --no-restore` passed. `dotnet test redis-vl-dotnet.sln --no-restore` passed with 25 Redis-backed integration tests skipped because `REDIS_VL_REDIS_URL` was not configured locally.
- Learnings for future iterations:
  - Patterns discovered: aggregation pipeline property references should resolve through the schema when they target indexed fields, but aliases created by `APPLY` or `REDUCE` need to remain usable in later `GROUPBY` and `SORTBY` stages without schema lookups.
  - Gotchas encountered: aggregation `LOAD` fields need `@field` and `field` inputs to dedupe to the same runtime property, otherwise command generation emits duplicate load arguments.
  - Useful context: there are still no `AGENTS.md` files under the edited paths, so reusable area guidance remains centralized in `progress.txt`.
---
